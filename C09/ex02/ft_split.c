char	*cut_str(char *str, char *charset, char *final)
	int i;
	int j;
	int k;
	
	i = 0;
	j = 0;
	k = 0;
	tant que str != 0:
		si str i == charset j
			tant que j != strlen(charset):
				j++;
			si j == strlen(charset)
				si tableau = malloc(sizeof(str) * k);
					str -> tableau
				

char	**ft_split(char *str, char *charset)
{
	char *final;

	while final = &function(str, charset)
		final[i] = function
	final = &function(str charset);
}
